# Root requirements for AI Runtime Security Framework
# Install component-specific requirements separately or use Docker Compose

# Core dependencies (if running without Docker)
fastapi==0.104.1
uvicorn[standard]==0.24.0
httpx==0.25.2
pydantic==2.5.0
python-dotenv==1.0.0
streamlit==1.28.1
requests==2.31.0

# Development dependencies (optional)
pytest==7.4.3
pytest-asyncio==0.21.1
black==23.12.0
flake8==6.1.0
mypy==1.7.1
